# Treasure 颜色配比设计方案

> 本文档定义 Treasure 的完整颜色配比方案。**不区分"平台"与"插件"**——所有组件、页面、应用共享同一套色彩语言。开发者在设计新功能或新插件时，遵循此方案即可与整体保持视觉一致。

---

## 一、设计理念

Treasure 采用 **暖调毛玻璃文人雅致风**——以亚麻/米色暖底为基底，紫色系为核心强调色，辅以暖金点缀，营造「宣纸上的紫墨」质感。

### 核心调性

| 要素 | 说明 |
|------|------|
| **暖色基底** | 所有背景以暖米/亚麻色族为主，非纯白或冷灰 |
| **毛玻璃层叠** | `backdrop-filter: blur(16~22px)` + 半透明背景营造空间深度 |
| **统一暖棕阴影** | 阴影一律使用 `rgba(122, 96, 77, ...)`，而非冷灰/纯黑 |
| **极低对比度分隔线** | `rgba(126, 108, 87, 0.08)` 几乎隐形 |
| **定制字体** | 行楷仅用于装饰性标题，品牌手写体仅用于品牌标识 |

### 两条主色调线

```
紫罗兰线（交互/强调）         暖金线（点缀/品牌）
─────────────────          ─────────────────
#3d3450  深紫底              #c9a04e  品牌古金
#7656a7  活跃紫              #e8c98a  暖金强调
#9a84bd  操作紫              #ffb978  暖橘
#b69cff  亮紫强调
#b190d5  薰衣草紫
```

---

## 二、色板

按色系组织，不区分平台与插件。每个色值均标注典型用途，开发者按需取用。

### 2.1 暖米中性色系

| 色值 | 典型用途 |
|------|---------|
| `#f3ecdf` | 背景、面板、卡片 |
| `#f5efe4` | 主内容区背景 |
| `#f7f0e5` | 预览器、内容展示区 |
| `#ece3d3` | 标签栏、功能区 |
| `#ede2d1` | 代码块、引用区 |

### 2.2 紫罗兰色系

| 色值 | 典型用途 |
|------|---------|
| `#3d3450` | 深色导航背景 |
| `#7656a7` | 活跃态、选中文字 |
| `#9a84bd` | 操作图标、按钮 |
| `#b69cff` | 亮色强调、装饰光效 |
| `#b190d5` | 装饰图标、辅助元素 |
| `#6d5b7f` | 分组标题（T6） |
| `#c9b8d9` | 导航文字 |
| `rgba(154, 132, 189, 0.22)` | 导航悬停背景 |
| `rgba(154, 132, 189, 0.34)` | 导航选中背景 |
| `#554861` | 欢迎页引言 |
| `#756982` | 副引言（同 T7） |

### 2.3 暖金色系

| 色值 | 典型用途 |
|------|---------|
| `#c9a04e` | 品牌文字、古金色标识 |
| `#e8c98a` | 强调装饰、下划线 |
| `#ffb978` | 暖橘光晕、渐变起始 |

### 2.4 Indigo 色系

| 色值 | 典型用途 |
|------|---------|
| `#6366f1` | 主交互色、按钮、选中态、进度条 |
| `#4f46e5` | 悬停态 |
| `#8b5cf6` | 渐变终点 |

### 2.5 功能语义色系

| 语义 | 色值 | 浅色背景 | 典型用途 |
|------|------|---------|---------|
| 危险/删除 | `#d36c6c` | `#fcf5f5` | 删除、逾期、紧急标记 |
| 成功/完成 | `#22c55e` | — | 已完成标记 |
| 警告/重要 | `#f97316` | `#fff7ed` | 重要标记 |

### 2.6 优先级色系

| 等级 | 色值 | 背景色 |
|------|------|--------|
| P0 紧急 | `#d36c6c` | `#fcf5f5` |
| P1 重要 | `#f97316` | `#fff7ed` |
| P2 一般 | `#6366f1` | `#eef2ff` |
| P3 低优 | `#9ca3af` | `#f9fafb` |

### 2.7 渐变与光晕

| 渐变 | 用途 |
|------|------|
| `radial-gradient(circle at 18% 12%, rgba(255,226,188,0.88) 0, transparent 32%)` + `radial-gradient(circle at 86% 18%, rgba(218,232,255,0.95) 0, transparent 28%)` + `linear-gradient(135deg, #fff7ea 0%, #f7f2ff 46%, #eef7ff 100%)` | 全局页面背景 |
| `linear-gradient(90deg, #ffb978, #b69cff)` | 橘→紫特征渐变：进度条、hover 背景、装饰条 |
| `#6366f1 → #8b5cf6` | Violet 渐变：进度条填充 |

### 2.8 阴影色

所有阴影统一使用暖棕色调，拒绝冷灰/纯黑。

| 色值 | 典型用途 |
|------|---------|
| `rgba(122, 96, 77, 0.14)` | 面板、卡片 |
| `rgba(122, 96, 77, 0.20)` | 弹窗、浮层 |
| `rgba(122, 96, 77, 0.12)` | 抽屉、浮层面板 |
| `rgba(122, 96, 77, 0.10)` | 卡片悬停 |
| `rgba(96, 74, 110, 0.18)` | 右键菜单 |
| `rgba(122, 96, 77, 0.16)` | 折叠按钮 |

### 2.9 边框与分隔线色

| 色值 | 典型用途 |
|------|---------|
| `rgba(126, 108, 87, 0.08)` | 极低对比度分隔线 |
| `rgba(126, 108, 87, 0.12)` | 通用边框 |
| `rgba(126, 108, 87, 0.16)` | 较明显边框 |

---

## 三、文字色阶

以紫棕暖底为基底，从深到浅构成 T1–T9 完整层级。不使用纯黑（`#000`）或纯灰（`#666`/`#999`）。

| 层级 | 色值 | 用途 | 说明 |
|------|------|------|------|
| **T1** | `#4f463b` | 标题、卡片标题 | 最深棕色，承载信息层级 |
| **T2** | `#4b4257` | 正文、标题 | 深紫棕，阅读舒适度最优 |
| **T3** | `#554d43` | 编辑器正文、预览器文字 | 深棕，略暖于 T2 |
| **T4** | `#5c5347` | 侧边栏、表单、次要文字 | 暖灰棕 |
| **T5** | `#655b4f` | 工具栏文字、按钮文字 | 暖棕灰 |
| **T6** | `#6d5b7f` | 分组标题、功能标题 | 紫灰，标志色 |
| **T7** | `#756982` / `#8b7e9a` | 描述信息、元信息 | 淡紫灰 |
| **T8** | `#9a8fa7` / `#9b8ca9` | 占位符、提示、空态 | 浅紫灰 |
| **T9** | `#9a8e7e` | 行号、极弱辅助信息 | 浅暖灰 |

> **设计原则**：紫棕暖底 + 渐次弱化，从 T1 到 T9 逐步降低对比度，确保整体视觉的暖调统一性。

---

## 四、装饰性标题

装饰性标题是文字色阶的补充——常规文字使用 T1–T9，装饰性标题使用特殊字体与色彩，营造文雅气息。

### 行楷装饰性标题

| 要素 | 内容 |
|------|------|
| 字体 | `"Ruhua Simeng", "Kaiti", "STKaiti", "楷体", cursive` |
| 色彩 | T6 `#6d5b7f`（紫灰，与紫色主色调线一致） |
| 字号字重 | 11px / 600 |
| 字距 | `letter-spacing: 0.08em` |
| 约束 | **仅限装饰性标题**，不可用于正文、按钮、标签等 |

### 品牌手写文字

| 要素 | 内容 |
|------|------|
| 字体 | Alex Brush 手写体 |
| 色彩 | `#c9a04e` 古金色 |

### 侧边栏标题下划线装饰

| 要素 | 内容 |
|------|------|
| 实现 | `::after` 伪元素 |
| 尺寸 | 34px × 3px |
| 圆角 | `border-radius: 999px` |
| 颜色 | `linear-gradient(90deg, #ffb978, #b69cff)` 橘→紫渐变 |

---

## 五、配色指引

### 5.1 色调关系

```
暖米色系（基底）── 占据 70% 以上面积
      │
      ├── 紫罗兰色系（主交互）── 按钮、图标、强调
      │       │
      │       └── Indigo 色系（变体）── 替代紫罗兰用于任务管理等场景
      │
      ├── 暖金色系（点缀）── 品牌文字、装饰线、光晕
      │
      └── 功能语义色系（信息）── 危险/成功/警告/优先级
```

### 5.2 按钮角色映射

四级按钮体系定义了操作角色与色系的对应关系：

| 角色 | 色系 | 渐变 | 阴影 |
|------|------|------|------|
| Primary（主操作） | 紫罗兰色系 | `linear-gradient(135deg, #b095e2, #8b6cc1)` | `rgba(139,108,193,0.25)` |
| Warning（特权操作） | 暖金色系 | `linear-gradient(135deg, #eecb8e, #d6a758)` | `rgba(214,167,88,0.25)` |
| Danger（危险操作） | 功能语义（危险） | `linear-gradient(135deg, #df7d7d, #c95e5e)` | `rgba(201,94,94,0.25)` |
| Default（次要操作） | 半透明白 + 暖棕边框 | 无渐变 | 无阴影 |

> 按钮的渐变和阴影由色系中的邻近色值推算得出，不脱离色板体系。

### 5.3 文字色阶与色系的搭配

| 色阶范围 | 搭配色系 | 场景 |
|---------|---------|------|
| T1–T3（深色端） | 暖米色系（背景） | 标题、正文区域 |
| T4–T5（中间色） | 暖米色系 + 紫罗兰色系 | 侧边栏、工具栏 |
| T6（紫色） | 紫罗兰色系 | 分组标题、功能标题 |
| T7–T9（浅色端） | 紫罗兰色系（浅色端） | 描述、提示、辅助信息 |
| 功能语义色 | 对应语义色系 | 按钮、标签、badge |

### 5.4 色温变体：冷感方案

以 Indigo 取代紫罗兰作为主交互色，辅以更白净的暖白面板（`#faf7f2`），形成偏冷硬但光线不刺眼的变体方案。适用于任务管理、数据分析等需要清晰结构感的场景。

**约束**：
- 阴影色、危险色、页面背景渐变不可偏离
- 冷灰边框（`#e5e7eb`）仅限输入框、按钮等需要明确边界的场景，分隔线仍使用 `rgba(126, 108, 87, 0.08)`

---

## 六、扩展指引

### 新增色值

- 优先从已有色系中选取
- 若确需引入新色值，必须在色板中同步新增并注明用途
- 新色值需符合暖色基底 + 暖棕阴影的调性约束

### 保持整体感

- 同一色系内的色值可自由组合
- 跨色系组合时，暖米色系始终作为基底
- 紫罗兰与 Indigo 不可在同一上下文中混用（选择其一作为主交互色）
- 暖金色系仅用于点缀，不超过总面积的 5%

### 注释与引用

- 字体引入：通过 `@import url('@treasure/sdk/ruhua-simeng.css')` 引入行楷字体
- 危险色：直接使用 `#d36c6c` / `#fcf5f5`
- 命名约定：`--treasure-{context}-{property}`

---

## 附录

### A.1 CSS 变量定义表

源文件：`src/style/common.css`

| 变量 | 色值 | 用途 |
|------|------|------|
| `--treasure-side-backgroud-color` | `#3d3450` | 深色导航背景 |
| `--treasure-bg-tabs` | `#ece3d3` | 标签栏背景 |
| `--treasure-background-color` | `#f5efe4` | 主内容区背景 |
| `--treasure-side-text` | `#c9b8d9` | 导航文字 |
| `--treasure-side-hover-bg` | `rgba(154, 132, 189, 0.22)` | 导航悬停背景 |
| `--treasure-side-active-bg` | `rgba(154, 132, 189, 0.34)` | 导航选中背景 |
| `--treasure-side-accent` | `#b69cff` | 导航强调色 |
| `--treasure-tab-text` | `#5c5347` | 标签文字 |
| `--treasure-tab-text-active` | `#7656a7` | 标签选中文字 |
| `--treasure-tab-accent` | `#e8c98a` | 标签强调色 |
| `--treasure-border-color` | `rgba(126, 108, 87, 0.12)` | 通用边框 |
| `--el-color-danger` | `#d36c6c` | 危险色覆盖 |
| `--el-color-primary` | `#6366f1` | Indigo 主色覆盖（冷感方案） |
| `--el-color-primary-light-3` | `#818cf8` | Indigo 浅色（冷感方案） |
| `--el-color-primary-dark-2` | `#4f46e5` | Indigo 深色（冷感方案） |

### A.2 色值计算关系

- `#fcf5f5`（危险浅色背景）：`#d36c6c` 以与 `#fef2f2` 对 `#ef4444` 同等的混合比例叠加在白色上
- 毛玻璃半透明背景：`rgba(243, 236, 223, 0.75)` + `backdrop-filter: blur(16px)` 实现暖米亚麻质感

### A.3 色彩关系映射

```
文字色阶 ← 用途 → 色系
─────────────────────────────────────
T1-T3  ──→ 标题/正文      ──→ 暖米色系（背景）+ 紫罗兰色系（文字）
T4-T5  ──→ 导航/工具栏     ──→ 紫罗兰色系 + 暖金色系
T6     ──→ 分组标题        ──→ 紫罗兰色系（装饰性标题引用）
T7-T9  ──→ 描述/提示/辅助   ──→ 紫罗兰色系（浅色端）
语义色  ──→ 按钮/标签/badge ──→ 功能语义色系 + 优先级色系
```